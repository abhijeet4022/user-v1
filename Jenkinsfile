// Load the shared library "roboshop" in the pipeline, as defined in the "Manage Jenkins" > "Global Pipeline Libraries" setting. By default, it will look for the `def call()` function under this demo shared library.
@Library('roboshop') _

demo()